---
import Nav from '../components/Nav.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'deb adalwolf',
  description = 'Backend & mobile developer. APIs y aplicaciones móviles con Python, Java, Flutter y Docker.',
} = Astro.props;

const canonical = new URL(Astro.url.pathname, Astro.site ?? 'https://klouds27.dev');
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="canonical" href={canonical} />

    <title>{title}</title>
    <meta name="description" content={description} />

    <meta property="og:type"        content="website" />
    <meta property="og:url"         content={canonical} />
    <meta property="og:title"       content={title} />
    <meta property="og:description" content={description} />

    <meta name="twitter:card"        content="summary" />
    <meta name="twitter:title"       content={title} />
    <meta name="twitter:description" content={description} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
  </head>
  <body>
    <Nav />
    <main>
      <slot />
    </main>
    <footer class="site-footer">
      <span class="site-footer__email">
        <a href="mailto:adolfogonzpe@gmail.com">adolfogonzpe@gmail.com</a>
      </span>
      <span class="site-footer__github">
        <a href="https://github.com/klouds27" target="_blank" rel="noopener">github.com/klouds27 ↗</a>
      </span>
    </footer>
  </body>
</html>

<style>
  .site-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 4rem;
    border-top: 1px solid var(--border);
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--fg-ghost);
  }

  .site-footer a {
    color: var(--fg-ghost);
    transition: color 0.15s ease;
  }

  .site-footer a:hover {
    color: var(--fg-dim);
  }

  @media (max-width: 640px) {
    .site-footer {
      flex-direction: column;
      gap: 0.5rem;
      padding: 1.5rem 1.5rem;
      text-align: center;
    }
  }
</style>
